import 'package:flutter/material.dart';

/*
Creating this file to store the constant values for the color.
- By creating the separate file for  the constant we can update the values in only one place and saves time.
*/

const Color KGreyColor = Colors.grey;
const Color KRedColor = Colors.red;
const Color KWhiteColor = Colors.white;

const headingTextStyle =
    TextStyle(fontSize: 22, color: KWhiteColor, fontWeight: FontWeight.bold);
